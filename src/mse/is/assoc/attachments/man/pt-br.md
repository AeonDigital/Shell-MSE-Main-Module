# Synopsis

Identifica quando a variável nomeada passada é um array associativo.



&nbsp;

# Parameters

## string varName

- aka       : -v --varName
- hint      :  
  Name of the variable to be tested.


&nbsp;

# Returns bool

`1` se `true` ou `0` se `false`.